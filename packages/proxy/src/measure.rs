//! count_tokens counterfactual measurement: probe the original request body,
//! record actually-billed usage, emit one savings-ledger row per request.
//! Also: cachePrefixSha8 bust attribution, static-tag churn canary.
