//! Structural feature extraction for brain-API scoring requests.
//! Emits vectors + features only — raw text never leaves this machine.
//! Must match the Python trainer's featurization vector-for-vector (§8.2).
