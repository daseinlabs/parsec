# Dev-only (next dev): point the hero's SavingsStat at a locally running
# platform service. Production builds bake NEXT_PUBLIC_PLATFORM_URL in from the
# deploy environment instead; when unset, the stat simply doesn't render.
NEXT_PUBLIC_PLATFORM_URL=http://127.0.0.1:8080
