# Production build (next build): the platform service the hero's SavingsStat
# fetches GET /savings/public from. Public URL, no secret — committed so the
# static export is reproducible. Keep in sync with the Cloud Run service
# (scripts/deploy_platform.sh prints it after each deploy).
NEXT_PUBLIC_PLATFORM_URL=https://dasein-platform-mrfpdhsaeq-uc.a.run.app
